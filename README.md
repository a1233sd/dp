# DiffPress — сервис дифф-аналитики лабораторных отчетов

DiffPress — это модульная система антиплагиата, которая индексирует лабораторные отчеты в PDF и визуализирует совпадения в формате, похожем на `git diff`. Сервис построен на Node.js/Next.js, хранит файлы на диске и результаты проверок в SQLite, поддерживает контейнеризацию через Docker и предоставляет REST API для автоматизации.

---

## Ключевые возможности

- 📥 Загрузка отчетов в PDF через REST API или веб-интерфейс
- 🔄 Асинхронная постановка проверок в очередь и хранение истории результатов
- 🧮 Сравнение текстов с использованием косинусного сходства
- 🗄️ Файлы сохраняются на смонтированном диске, индексы — в SQLite
- 🧩 UI показывает результаты проверки и визуальное сравнение с найденными совпадениями (аналог `git diff`)
- 📊 Отчеты о проверке доступны отдельным API по идентификатору
- 🐳 Готовый Dockerfile и docker-compose для развёртывания

---

## Архитектура проекта

```
.
├── Dockerfile                # Многостадийная сборка Next.js
├── docker-compose.yml        # Локальное развёртывание с томами для БД и хранилища
├── data/                     # SQLite база (создаётся автоматически)
├── src/
│   ├── app/
│   │   ├── api/              # REST API роуты (Next.js App Router)
│   │   ├── components/       # UI компоненты (DiffViewer и др.)
│   │   ├── layout.tsx        # Базовый layout приложения
│   │   └── page.tsx          # Главная страница с формой загрузки и diff UI
│   └── lib/                  # Логика работы с БД, хранилищем, очередью проверок
│       ├── check-processor.ts    # Очередь, расчёт сходства, генерация diff-preview
│       ├── config.ts             # Конфигурация путей, подготовка директорий
│       ├── db.ts                 # Подключение к SQLite через better-sqlite3
│       ├── repository.ts         # CRUD-операции над отчётами и проверками
│       ├── storage.ts            # Сохранение PDF на диск
│       └── text.ts               # Нормализация текста и косинусное сходство
└── vitest.config.ts         # Настройки тестов
```

---

## Требования

- Node.js 20+
- npm 10+
- Docker 24+ (для контейнерного запуска)

---

## Конфигурация

| Переменная              | Значение по умолчанию | Описание |
|-------------------------|-----------------------|----------|
| `REPORT_STORAGE_DIR`    | `<корень>/storage`    | Путь к каталогу на диске, куда сохраняются PDF. Диск должен существовать и быть доступным на запись. |

При первом запуске приложение создаст:
- директорию `storage/` (если переменная не задана — внутри проекта);
- файл базы данных `data/reports.sqlite`.

> 💾 **Важно:** если у вас уже есть внешний диск с архивом отчётов, смонтируйте его и передайте путь через `REPORT_STORAGE_DIR`. Приложение будет индексировать новые файлы в указанном каталоге, не удаляя существующие.

---

## Локальный запуск без Docker

```bash
npm install
npm run dev
```

По умолчанию фронтенд и REST API будут доступны на http://localhost:3000. Файлы будут сохраняться в `./storage`.

Для production-сборки:

```bash
npm run build
npm run start
```

---

## Запуск в Docker

1. Соберите контейнеры:
   ```bash
   docker compose build
   ```

2. Поднимите сервисы приложения, SQLite и веб-интерфейса к базе:
   ```bash
   docker compose up -d
   ```

3. Проверьте тома:
   - директория `storage/` в корне проекта смонтирована как `/data/reports` в контейнере `diffpress`;
   - файл базы `data/reports.sqlite` смонтирован в контейнерах `diffpress`, `sqlite` и `sqlite-ui` для сохранения истории.

4. UI приложения доступен на http://localhost:3000 (порт можно поменять через переменную `PORT`).

5. Веб-интерфейс для базы SQLite доступен на http://localhost:8080. Авторизация не требуется, база подключена автоматически через переменную `SQLITE_DATABASE`.

---

## REST API

### `POST /api/reports`
Загрузить PDF и поставить проверку в очередь.

- **Запрос:** `multipart/form-data` c полем `file` (PDF).
- **Ответ 202:**
  ```json
  {
    "reportId": "9d1c...",
    "checkId": "6cfb...",
    "status": "queued"
  }
  ```

### `GET /api/reports`
Получить список всех отчётов с последним статусом проверки.

- **Ответ 200:**
  ```json
  {
    "reports": [
      {
        "id": "...",
        "originalName": "lab1.pdf",
        "createdAt": "2024-05-01T12:00:00.000Z",
        "latestCheck": {
          "id": "...",
          "status": "completed",
          "similarity": 74.5,
          "createdAt": "..."
        }
      }
    ]
  }
  ```

### `GET /api/reports/{id}`
Подробная информация об отчёте и история проверок.

### `GET /api/checks/{id}`
Статус проверки и список совпадений.

- В поле `matches` приходит массив совпадений:
  ```json
  {
    "reportId": "...",
    "reportName": "lab0.pdf",
    "similarity": 64.12,
    "diffPreview": "+ Добавленный текст\n- Удалённый текст"
  }
  ```

### `GET /api/diff?source={reportId}&target={reportId}`
Возвращает полный diff между выбранными отчётами (используется UI для визуализации «как в git diff»).

---

## Обработка и алгоритм

1. **Загрузка** — файл сохраняется в `REPORT_STORAGE_DIR`, извлекается текст с помощью `pdf-parse`.
2. **Индексирование** — текст и метаданные попадают в SQLite.
3. **Очередь проверок** — создаётся запись в таблице `checks` со статусом `queued`, после чего фоновой воркер последовательно обрабатывает очередь.
4. **Подсчёт сходства** — для каждого ранее загруженного отчёта вычисляется косинусное сходство по bag-of-words.
5. **Diff** — для каждого совпадения строится краткий превью-дифф и полный diff через библиотеку `diff`.
6. **UI** — фронтенд опрашивает API, показывает статус, список совпадений и визуальный diff.

---

## Тестирование

Для unit-тестов используется Vitest. Покрытие включает проверку нормализации текста и расчёта косинусного сходства.

```bash
npm test
```

Добавьте собственные тесты для проверки ваших алгоритмов или интеграционных сценариев загрузки файлов.

---

## Как работает diff-интерфейс

- Левая колонка показывает найденные совпадения с процентом и текстовым превью (топ-10 строк diff-а).
- Правая колонка — полный diff выбранной пары отчётов с подсветкой добавленных (зелёные) и удалённых (красные) фрагментов.
- При выборе другого совпадения UI запрашивает `/api/diff` и перерисовывает представление без перезагрузки страницы.

---

## Расширение и интеграция

- **Хранилище**: замените SQLite на PostgreSQL — достаточно переписать модуль `repository.ts`.
- **Очередь**: при необходимости можно вынести воркер в отдельный сервис и общаться через Redis / RabbitMQ.
- **Алгоритм**: модуль `text.ts` изолирован — можно подменить косинусное сходство на более продвинутый метод (n-граммы, семантические векторы и т.п.).
- **API-ключи**: добавьте авторизацию (например, API tokens), если в будущем появятся требования по защите эндпоинтов.

---

## Скрипты npm

| Команда          | Назначение                         |
|------------------|------------------------------------|
| `npm run dev`    | dev-сервер Next.js                 |
| `npm run build`  | production-сборка                  |
| `npm run start`  | запуск собранного приложения       |
| `npm run lint`   | ESLint (конфигурация Next.js)      |
| `npm test`       | Vitest unit-тесты                  |

---

## Подготовка внешнего диска

1. Создайте каталог на диске, где будут храниться PDF (например, `/mnt/reports`).
2. Убедитесь, что процесс Node.js имеет права на запись.
3. При запуске установите переменную окружения: `REPORT_STORAGE_DIR=/mnt/reports npm run start`.
4. При использовании Docker подключите диск как том (`- /mnt/reports:/data/reports`).

---

## Лицензия

Проект предоставлен в образовательных целях. Адаптируйте и расширяйте под свои процессы проверки отчетов.
